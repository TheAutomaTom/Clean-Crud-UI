import type { AccountSpec } from "./AccountSpec";

export class AccountInfo {
  // User: User;
  AccountSpec: AccountSpec;

  constructor(
    // user: User,
    AccountSpec: AccountSpec
  ) {
    // this.User= user;
    this.AccountSpec = AccountSpec;

  }
}
